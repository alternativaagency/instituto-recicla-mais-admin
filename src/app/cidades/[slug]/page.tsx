/* eslint-disable @next/next/no-img-element */
import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { notFound } from "next/navigation";
import { getCity } from "@/lib/data";
export default async function CityPage({params}:{params:Promise<{slug:string}>}){ const {slug}=await params; const city=await getCity(slug); if(!city) notFound(); const metrics=[city.jobsdone!=null?[city.jobsdone.toLocaleString("pt-BR"),"ações realizadas"]:null,city.trashrecycledkg!=null?[`${city.trashrecycledkg.toLocaleString("pt-BR")} kg`,"resíduos reciclados"]:null].filter(Boolean) as string[][]; return <main id="conteudo"><section className="city-hero"><Link href="/cidades"><ArrowLeft/> Voltar para cidades</Link><p>{city.state}</p><h1>{city.cityname}</h1></section><section className="city-detail"><div><p className="section-kicker">Atuação local</p><h2>Trabalho construído no território</h2><p className="lead">{city.description || "As informações sobre esta atuação serão atualizadas pela equipe do Instituto."}</p></div>{metrics.length>0&&<aside>{metrics.map(([value,label])=><div key={label}><strong>{value}</strong><span>{label}</span></div>)}</aside>}</section>{city.images?.length?<section className="gallery" aria-label="Galeria da cidade">{city.images.map((src,i)=><div key={src}><img src={src} alt={`Registro de ${city.cityname} ${i+1}`} loading="lazy"/></div>)}</section>:null}</main> }
