"use client";
import { useState, useTransition } from "react"; import { useRouter } from "next/navigation"; import { requestOtp,verifyOtp,type LoginResult } from "./actions";
const initial:LoginResult={ok:false,stage:"email",message:""};
export function LoginForm(){const [email,setEmail]=useState("");const [token,setToken]=useState("");const [state,setState]=useState(initial);const [pending,start]=useTransition();const router=useRouter();
 function send(e:React.FormEvent){e.preventDefault();start(async()=>setState(await requestOtp(email)));}
 function verify(e:React.FormEvent){e.preventDefault();start(async()=>{const result=await verifyOtp(email,token);setState(result);if(result.ok){router.push("/admin");router.refresh();}})}
 return <div className="login-panel"><div><p className="eyebrow">Área administrativa</p><h1>Acesso seguro, sem senha.</h1><p>Use exclusivamente o e-mail autorizado pelo Instituto. Enviaremos um código de acesso temporário.</p></div>{state.stage==="code"?<form onSubmit={verify}><label>Código recebido<input inputMode="numeric" autoComplete="one-time-code" value={token} onChange={e=>setToken(e.target.value)} required minLength={6}/></label><button className="button primary" disabled={pending}>{pending?"Verificando...":"Entrar"}</button><button type="button" className="link-button" onClick={()=>setState(initial)}>Usar outro e-mail</button></form>:<form onSubmit={send}><label>E-mail autorizado<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button className="button primary" disabled={pending}>{pending?"Enviando...":"Receber código"}</button></form>}{state.message&&<p className={state.ok?"form-status success":"form-status error"} role="status">{state.message}</p>}</div>}
